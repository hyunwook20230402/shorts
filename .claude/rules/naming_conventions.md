- **엄격한 스네이크 케이스:** 모든 변수명과 함수명은 대문자를 배제하고 소문자와 언더바(`_`)만 사용하는 `snake_case` 스타일을 사용합니다.
- **예외 허용 (ML/DL):** 훈련 데이터를 구성할 때 입력 변수 `X`와 레이블 변수 `y`로 표기하는 것은 허용됩니다. 기타 딥러닝, 머신러닝 코드에서 관례적으로 대문자를 사용하는 수학적/모델적 표현(예: 행렬 표현 등)도 예외적으로 허용합니다.
- **함수명:** 반드시 동사로 시작하여 역할을 명확히 하세요. (예: `extract_text_from_image`, `generate_image_prompt`)
- **API 라우팅:** FastAPI 엔드포인트는 항상 `/api/v1/` 접두사를 포함해야 합니다.
